export class Skill {
    public id: string;
    public name: string;
    public imgPath: string;

    constructor(id: string, name: string, imgPath: string) {
        this.id = id;
        this.name = name;
        this.imgPath = imgPath;
    }
}
